{
  "name": "allow-hosts",
  "description": "Allows specific hosts / IP addresses to connect and fetch policy.",
  "type": "module",
  "provides": {
    "allow-hosts": {
      "description": "Allows specific hosts / IP addresses to connect and fetch policy.",
      "steps": ["input ./input.json def.json"],
      "input": [
        {
          "type": "list",
          "namespace": "default",
          "bundle": "def",
          "variable": "acl",
          "label": "Allowed hosts / IPs",
          "subtype": {
            "type": "string",
            "label": "Host/IP",
            "question": "Host / IP to allow"
          },
          "while": "Do you want to specify more hosts?"
        }
      ]
    }
  }
}
